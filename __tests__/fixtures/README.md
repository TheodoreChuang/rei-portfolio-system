# Test fixtures

- **sample-statement.pdf** — Minimal valid PDF used by `upload.integration.test.ts`. Content is irrelevant until Chunk 3 (LLM extraction). If missing, integration tests that need it will skip or fail.
